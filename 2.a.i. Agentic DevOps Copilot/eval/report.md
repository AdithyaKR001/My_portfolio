# DevOps Copilot — Evaluation Report (placeholder)

This file is generated by running the evaluation. It will be overwritten with real
results when you run:

```bash
make eval        # or: python -m eval.run_eval --threshold 0.8
```

(Requires an LLM — set OPENAI_API_KEY, or configure Ollama, in `.env`.)
