apiVersion: v1
kind: ConfigMap
metadata:
  name: model-server-config
data:
  BACKEND: "mock" # swap to "ollama" once a real embedding model is reachable in-cluster
  BATCH_MAX_SIZE: "16"
  BATCH_MAX_WAIT_MS: "20"
  CACHE_MAX_SIZE: "2000"
  CACHE_TTL_SECONDS: "300"
